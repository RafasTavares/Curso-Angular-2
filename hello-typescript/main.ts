var minhaVar = 'minha variavel';

//ES 6 ES 2015
let num = 2;
const PI = 3.14;

//JS
var numeros = [1,2,3];
numeros.map(function(valor ){ return valor* 2;})

//Arrow functions
var numeros = [1,2,3];
numeros.map(valor =>  valor * 2);

class Matematica { 
    soma(x,y){
        return x + y;
    }
}